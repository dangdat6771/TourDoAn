╔════════════════════════════════════════════════════════════════════════════════════╗
║                      QUICK FIX REFERENCE - initdb.sql                              ║
║              3 Critical Issues Found & Fixed - Deployment Blockers                 ║
╚════════════════════════════════════════════════════════════════════════════════════╝

════════════════════════════════════════════════════════════════════════════════════
ISSUE #1: coupon_usage.order_id DATA TYPE MISMATCH (INTEGER → BIGINT)
════════════════════════════════════════════════════════════════════════════════════

File: Tour-Repo-BE/travel-api/initdb.sql
Line: ~53-58

❌ CURRENT (WRONG):
───────────────────
CREATE TABLE IF NOT EXISTS public.coupon_usage
(
    id integer NOT NULL GENERATED BY DEFAULT AS IDENTITY ( INCREMENT 1 START 1 MINVALUE 1 MAXVALUE 2147483647 CACHE 1 ),
    discount_amount numeric(15, 2) NOT NULL,
    order_id integer NOT NULL,          ← WRONG: Should be bigint!
    used_at timestamp(6) without time zone,
    coupon_id bigint NOT NULL,
    user_id bigint,
    CONSTRAINT coupon_usage_pkey PRIMARY KEY (id)
);

✅ CORRECTED:
─────────────
CREATE TABLE IF NOT EXISTS public.coupon_usage
(
    id integer NOT NULL GENERATED BY DEFAULT AS IDENTITY ( INCREMENT 1 START 1 MINVALUE 1 MAXVALUE 2147483647 CACHE 1 ),
    discount_amount numeric(15, 2) NOT NULL,
    order_id bigint NOT NULL,           ← FIXED: Changed to bigint
    used_at timestamp(6) without time zone,
    coupon_id bigint NOT NULL,
    user_id bigint,
    CONSTRAINT coupon_usage_pkey PRIMARY KEY (id)
);

WHY: orders.id is bigint, so coupon_usage.order_id must also be bigint
     PostgreSQL FK constraint requires matching data types

═════════════════════════════════════════════════════════════════════════════════════
ISSUE #2: MISSING FOREIGN KEY - cart.tour_schedule_id
═════════════════════════════════════════════════════════════════════════════════════

File: Tour-Repo-BE/travel-api/initdb.sql
Line: ~270-310 (In the ALTER TABLE statements section)

❌ CURRENT (INCOMPLETE):
────────────────────────
-- FK constraints for cart table:
ALTER TABLE IF EXISTS public.cart
    ADD CONSTRAINT fk8sa76crlbhwjnctf7u1j6pl0r FOREIGN KEY (tour_id)
    REFERENCES public.tours (id) MATCH SIMPLE
    ON UPDATE NO ACTION
       ON DELETE NO ACTION;

ALTER TABLE IF EXISTS public.cart
    ADD CONSTRAINT fkg5uhi8vpsuy0lgloxk2h4w5o6 FOREIGN KEY (user_id)
    REFERENCES public.users (id) MATCH SIMPLE
    ON UPDATE NO ACTION
       ON DELETE NO ACTION;

-- ❌ MISSING: No FK for tour_schedule_id!

✅ ADD THIS CONSTRAINT:
───────────────────────
ALTER TABLE IF EXISTS public.cart
    ADD CONSTRAINT fk_cart_tour_schedule_id FOREIGN KEY (tour_schedule_id)
    REFERENCES public.tour_schedules (id) MATCH SIMPLE
    ON UPDATE NO ACTION
       ON DELETE NO ACTION;

INSERT LOCATION: After the user_id constraint, before the categories ALTER statement

WHY: cart.tour_schedule_id column exists but has no referential integrity
     This allows orphaned schedule IDs; FK constraint prevents that

═════════════════════════════════════════════════════════════════════════════════════
ISSUE #3: MISSING FOREIGN KEY - coupon_usage.order_id
═════════════════════════════════════════════════════════════════════════════════════

File: Tour-Repo-BE/travel-api/initdb.sql
Line: ~310-330 (In the ALTER TABLE statements section)

❌ CURRENT (INCOMPLETE):
────────────────────────
-- FK constraints for coupon_usage table:
ALTER TABLE IF EXISTS public.coupon_usage
    ADD CONSTRAINT fkcsx5pbxjnp6jvuveb3lxyklhe FOREIGN KEY (user_id)
    REFERENCES public.users (id) MATCH SIMPLE
    ON UPDATE NO ACTION
       ON DELETE NO ACTION;

ALTER TABLE IF EXISTS public.coupon_usage
    ADD CONSTRAINT fkof9h3rw1vu403j5dbehr45ts2 FOREIGN KEY (coupon_id)
    REFERENCES public.coupons (id) MATCH SIMPLE
    ON UPDATE NO ACTION
       ON DELETE NO ACTION;

-- ❌ MISSING: No FK for order_id! (CRITICAL for data integrity)

✅ ADD THIS CONSTRAINT:
───────────────────────
ALTER TABLE IF EXISTS public.coupon_usage
    ADD CONSTRAINT fk_coupon_usage_order_id FOREIGN KEY (order_id)
    REFERENCES public.orders (id) MATCH SIMPLE
    ON UPDATE NO ACTION
       ON DELETE NO ACTION;

INSERT LOCATION: After the coupon_id constraint, before the coupons ALTER statement

WHY: coupon_usage.order_id column exists but has no referential integrity
     This allows orphaned order IDs; FK constraint prevents that
     Prevents deletion of orders referenced by coupon_usage records

════════════════════════════════════════════════════════════════════════════════════
IMPLEMENTATION STEPS
════════════════════════════════════════════════════════════════════════════════════

Option 1: EDIT initdb.sql BEFORE FIRST DEPLOYMENT
──────────────────────────────────────────────────
1. Open: Tour-Repo-BE/travel-api/initdb.sql
2. Find line ~53-58 with coupon_usage table definition
3. Change: order_id integer → order_id bigint
4. Find line ~270-310 with cart FK constraints  
5. Add: FK constraint for tour_schedule_id
6. Find line ~310-330 with coupon_usage FK constraints
7. Add: FK constraint for order_id
8. Save file and restart docker


Option 2: RUN DATABASE_FIXES.sql AFTER INITIAL DEPLOYMENT
────────────────────────────────────────────────────────
1. Deploy with current schema (will have issues)
2. Connect to PostgreSQL: psql -U postgres -d tour_service_db
3. Run: \i DATABASE_FIXES.sql
4. Restart backend service


Option 3: MANUAL SQL FIX (One-time fix)
──────────────────────────────────────
psql -U postgres -d tour_service_db

-- Fix #1: Data type
ALTER TABLE public.coupon_usage ALTER COLUMN order_id TYPE bigint;

-- Fix #2: Add cart FK
ALTER TABLE public.cart 
  ADD CONSTRAINT fk_cart_tour_schedule_id 
    FOREIGN KEY (tour_schedule_id) REFERENCES public.tour_schedules(id);

-- Fix #3: Add coupon_usage FK
ALTER TABLE public.coupon_usage 
  ADD CONSTRAINT fk_coupon_usage_order_id 
    FOREIGN KEY (order_id) REFERENCES public.orders(id);

════════════════════════════════════════════════════════════════════════════════════
VERIFICATION QUERIES (Run after fixes)
════════════════════════════════════════════════════════════════════════════════════

-- Verify FK constraints are in place
SELECT 
  tc.table_name, 
  kcu.column_name, 
  ccu.table_name AS foreign_table_name,
  ccu.column_name AS foreign_column_name
FROM information_schema.table_constraints AS tc 
  JOIN information_schema.key_column_usage AS kcu ON tc.constraint_name = kcu.constraint_name
  JOIN information_schema.constraint_column_usage AS ccu ON ccu.constraint_name = tc.constraint_name
WHERE tc.constraint_type = 'FOREIGN KEY' 
  AND tc.table_name IN ('cart', 'coupon_usage')
ORDER BY tc.table_name, kcu.column_name;

-- Verify coupon_usage.order_id is bigint
SELECT column_name, data_type, is_nullable 
FROM information_schema.columns 
WHERE table_name = 'coupon_usage' AND column_name = 'order_id';

-- Should return: order_id | bigint | NO (not null)

════════════════════════════════════════════════════════════════════════════════════
EXPECTED OUTPUT AFTER FIXES
════════════════════════════════════════════════════════════════════════════════════

FK Constraints Table:
  table_name   | column_name        | foreign_table_name | foreign_column_name
───────────────────────────────────────────────────────────────────────────────────
  cart         | tour_id            | tours              | id
  cart         | user_id            | users              | id
  cart         | tour_schedule_id   | tour_schedules     | id  ✅ NEW
  coupon_usage | user_id            | users              | id
  coupon_usage | coupon_id          | coupons            | id
  coupon_usage | order_id           | orders             | id  ✅ NEW

Data Type Check:
  column_name | data_type | is_nullable
───────────────────────────────────────
  order_id    | bigint    | NO          ✅ FIXED (was integer)

════════════════════════════════════════════════════════════════════════════════════
DEPLOYMENT STATUS AFTER FIXES
════════════════════════════════════════════════════════════════════════════════════

Before Fixes:  ❌ NOT DEPLOYABLE (3 critical issues blocking)
After Fixes:   ✅ READY FOR: docker-compose up --build

All other validations already passed:
  ✅ Java backend compilation
  ✅ React frontend build
  ✅ Docker configuration
  ✅ Environment setup
  
════════════════════════════════════════════════════════════════════════════════════
